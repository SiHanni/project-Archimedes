"""
심사 제출 보고서(final) 생성 — 문체 변환 · 제목 정리 · 절 추가를 한 번에 수행한다.

이 스크립트가 존재하는 이유: 보고서를 여러 차례 수동으로 손보다가 중간 산출물이
삭제되면 처음부터 다시 해야 한다. 실제로 그런 일이 있었다. 그래서 원본 한 벌에서
최종본까지 가는 경로를 **한 파일에 고정**해 두고, 언제든 같은 결과를 재생산한다.

입력  /tmp/pre_academic.html   (0장 + 1~11장, 구어체·원 제목 상태)
출력  ~/Desktop/TIPS 기능 심사 final.html

수행 순서
  1) 제목·판번호·근거 답안지 명기
  2) 산문 재작성 (기계 문체로 읽히던 문단)
  3) 문체 변환: 구어체 → 논문체(~하였다 / ~한다) 및 어휘 격상
  4) 소제목 정리: 안내조·메타 표현 제거
  5) 절 추가: 0.1.1 태그별 성능 · mIoU 분포 · 12장 정답 데이터 기준 · 13장 재현 절차
  6) 자동 변환이 만든 비문 교정
"""

from __future__ import annotations

import base64
import json
import re
from pathlib import Path

SRC = Path("/tmp/pre_academic.html")
SPEC = Path("/Users/sihwan/project-Archimedes/web/public/spec")
DST = Path.home() / "Desktop" / "TIPS 기능 심사 final.html"
STATS = Path("/tmp/miou_stats.json")
NLP = Path("/tmp/nlp_recalc.json")


def rewrite_prose(s: str) -> str:
    """기계 문체로 읽히던 문단을 사람이 쓴 문장으로 바꾼다."""
    R = [
        ("""  학습된 인공지능이 <b>"사진에서 눈에 띄는 물체"</b>를 먼저 찾아내고,
  그 다음 색·밝기·반사 특성을 이용해 <b>귀금속이 아닌 것(케이스·저울·포장상자)을 걷어냅니다.</b>
  마지막으로 반지 구멍처럼 <b>배경이 비쳐 보이는 부분</b>을 파냅니다.""",
         """  먼저 학습된 분할 모델이 사진에서 두드러진 물체를 찾는다. 이 단계만으로는 케이스나
  전자저울, 포장상자까지 함께 잡히므로, 색상과 밝기 그리고 금속 특유의 반사를 이용해
  귀금속이 아닌 부분을 제거한다. 마지막으로 반지 구멍처럼 뒤 배경이 비쳐 보이는 영역을
  따로 분리한다."""),
        ("""  ① 축별 모델 이름 · 학습 데이터셋 출처 · 학습/검증/테스트 개수 · 데이터 수집 기간<br>
  ② 단계별 연산 과정 &nbsp;·&nbsp; ③ 실사진 10건의 픽셀 결과와 거리 결과""",
         """  시험항목별로 사용한 모델의 이름과 학습 데이터 출처, 학습·검증·테스트 표본 수,
  데이터 수집 기간을 정리하였다. 이어서 각 기능의 단계별 연산 과정을 기술하고,
  실제 거래 사진 10건에 대한 픽셀 단위 측정 결과와 거리 산출 결과를 수록하였다."""),
        ("""  BiRefNet·Depth Pro 는 <b>공개된 사전학습 모델</b>이며 본 과제에서 <b>재학습하지 않았습니다.</b>
  위 표의 학습/검증/테스트 수치는 <b>원저작자가 공개한 값</b>입니다.<br><br>
  본 과제의 기여는 다음 셋입니다 — <b>(1) 자체 평가셋 구축</b>(정답 마스크·정답 태그),
  <b>(2) 판정 파라미터 9종의 실측 보정</b>, <b>(3) 후처리 4단계 신규 개발</b>
  (크롭 재추론 · 비귀금속 제외 · 포장 파내기 · 구멍 파내기).""",
         """  BiRefNet 과 Depth Pro 는 공개된 사전학습 모델을 그대로 도입한 것으로, 본 과제에서
  재학습을 수행하지 아니하였다. 위 표의 학습·검증·테스트 수치는 원저작자가 공개한 값을
  인용한 것이다.<br><br>
  본 과제에서 직접 수행한 작업은 세 가지다. 첫째, 정답 데이터를 자체적으로 구축하였다.
  사진 10건은 수작업으로 누끼를 따 정답 마스크를 제작하였고, 리뷰 100건은 태그를 직접
  분류하였다. 둘째, 판정에 쓰이는 임계값 9종을 이 데이터로 재설정하였다. 외부에서
  제안받은 값을 그대로 적용하면 금 제품까지 배경으로 제외되는 문제가 발생하였다.
  셋째, 모델 출력 뒤에 붙는 후처리 네 단계를 신규 개발하였다. 크롭 재추론, 비귀금속
  덩어리 제외, 포장 제거, 투과 영역 제거가 이에 해당한다."""),
        ("""  결과는 <b>Precision 79.03%</b>(겹별 79.2% ± 7.1%p)로 기준 87.3%에 미달했습니다.
          원인은 <b>부정 리뷰가 전체에서 5건뿐</b>이라는 데이터 부족이며, 클래스 가중치 20배와
          임계값 조정으로도 부정 태그 재현율이 0에 머물렀습니다. 이에 따라 운영 모델로는
          상용 API를 채택했고, 자체 모델은 부정 표본이 확보되는 대로 재시도합니다.""",
         """  Precision 은 79.03% 로 기준 87.3% 에 미치지 못하였다. 겹별 편차는 79.2% ± 7.1%p 이다.<br><br>
          미달의 원인은 모델이 아니라 데이터에 있다. 부정 리뷰가 전체 100건 중 5건뿐이라
          학습이 성립하지 않는다. 클래스 가중치를 20배까지 상향하고 태그별 임계값도
          조정하였으나 부정 태그의 재현율은 0 에서 개선되지 아니하였다. 이에 따라 운영
          모델로는 상용 API 를 채택하였으며, 자체 모델은 부정 표본이 확보되는 대로
          재시도할 예정이다."""),
        ("""  정답 점수를 <b>같은 규칙으로 산출</b>했기 때문이며 예측 성능이 완벽하다는 뜻이 아닙니다.
          규칙이 결정론적이므로 동일 입력에 동일 출력이 나오는 것이 당연합니다.
          이 항목이 검증하는 것은 <b>구현이 합의된 규칙을 정확히 따르는가</b>입니다.""",
         """  정답 점수를 같은 규칙으로 계산하였기 때문에 나온 값이며, 예측 성능이 완벽하다는
          의미는 아니다. 규칙이 결정론적이므로 동일 입력에는 항상 동일 출력이 산출된다.
          이 항목에서 확인하는 것은 구현이 합의된 규칙을 정확히 따르는지 여부이다."""),
        ("""  시험항목 4(매칭)에도 ‘거리’가 나오지만 <b>전혀 다른 값</b>입니다 —
  항목 4 는 <b>고객↔금은방</b> 직선거리(km, 좌표로 확정), 본 항목은
  <b>카메라↔귀금속</b> 거리(cm, 크기 가정 기반)입니다.
  <b>정답을 실측한 표본이 없어 정확도는 산출하지 않았으며</b>, 아래는 계산값과
  그 불확실성 범위입니다.""",
         """  시험항목 4 에도 거리가 나오지만 서로 다른 값이다. 항목 4 는 고객과 금은방 사이의
  직선거리로, 좌표가 있으므로 값이 확정된다. 본 항목에서 다루는 것은 카메라와 귀금속
  사이의 거리이며, 물체의 실제 크기를 가정해 역산한 추정값이다.<br><br>
  실제 거리를 측정한 표본이 없어 정확도는 산출하지 아니하였다. 아래 표의 값은 계산
  결과와 그 불확실성 범위이다."""),
    ]
    for a, b in R:
        s = s.replace(a, b)
    return s


# 구어체 → 논문체. 긴 문자열부터 적용해야 부분 치환으로 깨지지 않는다.
TONE = [
    ("되었습니다", "되었다"), ("했습니다", "하였다"), ("됩니다", "된다"), ("입니다", "이다"),
    ("합니다", "한다"), ("됐습니다", "되었다"), ("없습니다", "없다"), ("있습니다", "있다"),
    ("같습니다", "같다"), ("옵니다", "온다"), ("납니다", "난다"), ("줍니다", "준다"),
    ("씁니다", "쓴다"), ("냅니다", "낸다"), ("봅니다", "본다"), ("아닙니다", "아니다"),
    ("습니다", "다"),
    ("측정했다", "측정하였다"), ("제작했다", "제작하였다"), ("분류했다", "분류하였다"),
    ("적용했다", "적용하였다"), ("확인했다", "확인하였다"), ("사용했다", "사용하였다"),
    ("인용했다", "인용하였다"), ("정리했다", "정리하였다"), ("비교했다", "비교하였다"),
    ("학습했다", "학습하였다"), ("조정했다", "조정하였다"), ("제거했다", "제거하였다"),
    ("채택했다", "채택하였다"), ("만들었다", "구축하였다"), ("찾았다", "확인하였다"),
    ("고쳤다", "수정하였다"), ("됐다", "되었다"), ("했고", "하였고"), ("했으며", "하였으며"),
    ("했으나", "하였으나"), ("썼다", "사용하였다"), ("정했다", "산정하였다"),
    ("가른다", "구분한다"), ("걷어낸다", "제거한다"), ("파냅니다", "분리한다"),
    ("파낸다", "분리한다"), ("잡는다", "검출한다"), ("찾는다", "탐색한다"),
    ("고른다", "선정한다"), ("센다", "계수한다"), ("낸다", "산출한다"), ("쓴다", "사용한다"),
    ("건너뛴다", "생략한다"), ("되돌린다", "복원한다"), ("버린다", "기각한다"),
    ("나눈다", "분할한다"), ("더한다", "합산한다"), ("잰다", "측정한다"),
    ("수록했다", "수록하였다"), ("미치지 못했다", "미치지 못하였다"),
    ("올리고", "상향하고"), ("조정해 봤으나", "조정하였으나"),
    ("움직이지 않았다", "개선되지 아니하였다"), ("쓰기로 했다", "사용하기로 하였다"),
    ("분리에 실패</b>했다", "분리에 실패</b>하였다"),
    ("조건을 넣었다가", "조건을 적용하였다가"), ("반지가 퇴행했다", "반지의 성능이 저하되었다"),
    ("정확도를 숫자로 검증하지 못했다", "정확도를 정량적으로 검증하지 못하였다"),
    ("100% 모델 추정에 의존했다", "전적으로 모델 추정에 의존하였다"),
    ("셋 다 기각했다", "세 방법 모두 기각하였다"),
    ("검토하였으나 채택하지 않은 방법", "검토 후 기각한 방법"),
    ("개발 중 실패 사례", "적용 후 철회한 사례"),
    ("실제 거리를 잰 값이 없어", "실제 거리를 측정한 값이 없어"),
    ("표본을 늘려 보정할 계획이다", "표본을 확대하여 보정할 예정이다"),
    ("원인으로 보이다", "원인으로 판단된다"),
    ("""mIoU(정답 마스크와의 겹침 비율)는 정답 라벨이 없어 산출하지 않았다.
  정답 마스크가 확보되면 즉시 계산해 보고드리겠다.""",
     """mIoU 는 수작업 정답 마스크 10건과 대조하여 산출하였으며, 그 결과는 0.4 절에
  수록하였다."""),
    ("정답 마스크가 확보되면 즉시 계산해 보고드리겠다", "산출 절차는 13.4 절에 기술하였다"),
    ("mIoU(정답 마스크와의 겹침 비율)는 정답 라벨이 없어 산출하지 않았다",
     "mIoU 는 수작업 정답 마스크 10건과 대조하여 산출하였다"),
    # ── ~집니다 계열(피동) : 위 규칙이 못 잡던 어미 ──
    ("사라집니다", "사라진다"), ("걸러집니다", "걸러진다"), ("부서집니다", "부서진다"),
    ("만들어집니다", "만들어진다"), ("짧아집니다", "짧아진다"), ("길어집니다", "길어진다"),
    ("작아집니다", "작아진다"), ("커집니다", "커진다"), ("좁아집니다", "좁아진다"),
    ("넓어집니다", "넓어진다"), ("나뉩니다", "나뉜다"), ("맺힙니다", "맺힌다"),
    ("찍힙니다", "찍힌다"), ("높아집니다", "높아진다"), ("낮아집니다", "낮아진다"),
    ("달라집니다", "달라진다"), ("줄어듭니다", "줄어든다"), ("늘어납니다", "늘어난다"),
    ("집니다", "진다"),
    # ── '않습니다 → 않다' 로 잘못 바뀐 비문 복원 ──
    ("잡지 않다.", "잡지 않는다."), ("구분되지 않다.", "구분되지 않는다."),
    ("건드리지 않다.", "건드리지 않는다."), ("빨라지지 않다.", "빨라지지 않는다."),
    ("쓰지 않다.", "사용하지 않는다."), ("산출하지 않다.", "산출하지 않는다."),
    ("상을 맺다.", "상을 맺는다."), ("사용하지 않다.", "사용하지 않는다."),
    ("나오지 않다.", "나오지 않는다."), ("하지 않다.", "하지 않는다."),
    ("되지 않다.", "되지 않는다."), ("있지 않다.", "있지 않는다."),
    ("주장하지 않다.", "주장하지 않는다."), ("참조하지 않다.", "참조하지 않는다."),
    ("관여하지 않다.", "관여하지 않는다."), ("변하지 않다.", "변하지 않는다."),
    # ── <br> 로 잘려 이전 규칙이 놓치던 잔여 구어체 ──
    ("판정해 버립니다", "판정하여 제외한다"), ("보호되어 버립니다", "보호된다"),
    ("잡힙니다", "검출된다"), ("큽니다", "크다"), ("돌립니다", "수행한다"),
    ("살립니다", "보존한다"), ("가립니다", "가린다"), ("잽니다", "측정한다"),
    ("합칩니다", "합산한다"), ("나눕니다", "나눈다"), ("만듭니다", "생성한다"),
    ("다릅니다", "다르다"), ("못 가립니다", "구분할 수 없다"), ("가립니다", "구분한다"),
    ("경계를 훨씬 정확히 잡다", "경계를 보다 정확히 검출한다"),
    ("모델이 용기를 잡다", "모델이 용기를 검출한다"),
    ("모델에 한 번 더 넣다", "모델에 재입력한다"),
    ("10건 중 6장이", "10건 중 6건이"),
    ("계산했기 때문에 나온 값이지", "계산하였기 때문에 산출된 값이며"),
    ("신호로 본다", "신호로 판단한다"),
    ("개선됐으나 여전히 큽니다", "개선되었으나 여전히 크다"),
    ("작은 물체일수록 이득이 큽니다", "작은 물체일수록 개선 폭이 크다"),
    ("사람 눈의 색 차이에 가까운", "사람의 색 지각에 근접한"),
    ("사람이 눈으로 검수", "육안 검수"),
    ("실제 거리를 자로 잰 표본이 없어", "실제 거리를 측정한 표본이 없어"),
    ("통째로 배경이 된다", "전부 배경으로 분류된다"),
    ("통째로 보호된다", "전체가 보존된다"),
    ("해 주세요", "할 것"), ("보세요", "확인"),
]

HEADINGS = [
    ("심사 요구사항 대응표", "시험항목 대응"),
    ("자체 모델 학습 시도 — 채택하지 않음", "자체 모델 학습 시도 및 미채택 사유"),
    ("시험항목 2 — 실사진 10건 픽셀 결과", "시험항목 2 픽셀 측정 결과"),
    ("실사진 10건 거리 결과", "거리 산출 결과"),
    ("전체 처리 흐름", "처리 흐름"),
    ("왜 색만으로는 안 되는가 — 방식 선정 근거", "방식 선정 근거"),
    ("모델 두 개를 같은 사진으로 비교", "후보 모델 비교"),
    ("모델 추론 — 확률 계산식", "모델 추론 및 확률 산출"),
    ("2차 추론 — 잘라서 다시 보기", "2차 추론(크롭 재추론)"),
    ("효과 ① 용기를 배경으로 내린다", "효과 1. 용기의 배경화"),
    ("효과 ② 경계를 다시 잘 본다", "효과 2. 경계 정밀도 향상"),
    ("귀금속 판정 — 두 가지 신호", "귀금속 판정 지표"),
    ("기준값을 어떻게 정했나", "기준값 산정 근거"),
    ("안전장치 두 겹", "안전장치"),
    ("① 모폴로지 열기 — 하이라이트 걸러내기", "1. 모폴로지 열기에 의한 하이라이트 제거"),
    ("② 정반사 비율 — 금속 보호", "2. 정반사 비율에 의한 금속 보존"),
    ("제품에 붙은 포장 파내기", "제품 밀착 포장 제거"),
    ("색상(Hue)이 결정적 단서", "색상(Hue) 기반 판별"),
    ("비쳐 보이는 구멍 파내기", "투과 영역 제거"),
    ("왜 앞의 규칙을 그대로 못 쓰나", "기존 규칙을 적용할 수 없는 이유"),
    ("판별 기준 — 물체 바깥 배경과 같은 색인가", "판별 기준"),
    ("산출물 생성", "산출물"),
    ("구멍을 살리는 폴리곤 추출", "구멍을 보존하는 폴리곤 추출"),
    ("실측 결과 (10장 전수)", "실측 결과(10건 전수)"),
    ("거리 추정 — 사진 한 장으로 어떻게 재는가", "거리 추정"),
    ("카메라가 크기를 줄이는 방식", "결상 원리"),
    ("⭐ 왜 크기를 “가정”해야만 하는가 — 심사 핵심", "크기 가정이 필요한 이유"),
    ("세 값을 각각 어떻게 얻는가", "각 변수의 산출 방법"),
    ("실제 계산 예시 — 반지 사진", "산출 예시"),
    ("오차는 어떻게 계산되는가", "오차 산출"),
    ("결과를 “점”이 아니라 “범위”로 내는 이유", "범위 제시의 근거"),
    ("개발 중 찾은 결함 — 거리가 짧게 나오던 원인", "결함 및 수정: 거리 과소 산출"),
    ("남은 한계 — 심사에서 먼저 밝힐 것", "한계"),
    ("참고 — 크기·무게 측정 경로의 공식", "참고: 중량·시세 산출 경로"),
    ("검토했으나 채택하지 않은 방법", "검토 후 기각한 방법"),
    ("투명 케이스가 제품을 감싼 경우", "투명 케이스 밀착 시"),
    ("10.2 왜 크기를 “가정”해야만 하는가 — 심사 핵심", "10.2 크기 가정이 필요한 이유"),
    ("판별 기준: 물체 바깥 배경과 같은 색인가", "판별 기준"),
    ("10.1 카메라가 크기를 줄이는 방식", "10.1 결상 원리"),
    ("10.3 세 값을 각각 어떻게 얻는가", "10.3 각 변수의 산출 방법"),
    ("10.4 실제 계산 예시 — 반지 사진", "10.4 산출 예시"),
    ("10.5 오차는 어떻게 계산되는가", "10.5 오차 산출"),
    ("10.6 결과를 “점”이 아니라 “범위”로 내는 이유", "10.6 범위 제시의 근거"),
    ("10.7 개발 중 찾은 결함 — 거리가 짧게 나오던 원인", "10.7 결함 및 수정: 거리 과소 산출"),
    ("10.8 남은 한계 — 심사에서 먼저 밝힐 것", "10.8 한계"),
]

# 강조 박스 제목 중 안내조인 것
NOTE_TITLES = [
    ("한 줄 요약", "개요"), ("본 문서가 답하는 것", "문서 범위"),
    ("일상어로", "기하학적 의미"), ("이 표가 말해 주는 것", "해석"),
    ("이 진단이 맞다는 결정적 증거", "진단 근거"), ("이 진단이 맞다는 증거", "진단 근거"),
    ("사전학습 모델과 자체 기여를 구분합니다", "사전학습 모델과 자체 기여의 구분"),
    ("거리 추정은 완료판정 시험 대상이 아닙니다", "거리 추정의 시험 범위"),
    ("결과 — Precision 79.03% (기준 87.3% 미달)", "측정 결과"),
    ("이 항목은 머신러닝 모델이 아닙니다", "항목 성격"),
    ("왜 bbox 를 쓰면 안 되는가", "bbox 를 쓰지 않는 이유"),
    ("Depth Pro 는 “깊이”가 아니라 “초점거리”만 씁니다", "Depth Pro 의 사용 범위"),
    ("수학적으로 풀리지 않습니다 (단안 스케일 모호성)", "단안 스케일 모호성"),
    ("최소·최대 정규화를 쓰면 안 됩니다", "최소·최대 정규화 금지"),
    ("반드시 ‘또는’이어야 합니다", "조건이 ‘또는’ 이어야 하는 이유"),
    ("이 조건이 없을 때", "조건 미적용 시"),
    ("이 안전장치가 없을 때 무슨 일이 일어났나", "안전장치 미적용 시"),
    ("임계값 조정으로는 해결되지 않습니다", "임계값 조정으로 해결되지 않는 이유"),
    ("왜 앞의 규칙을 그대로 못 쓰나", "기존 규칙을 적용할 수 없는 이유"),
    ("실측 — 오작동 없음", "오작동 검증"),
    ("정확도 지표에 관하여", "정확도 지표"),
]

# 자동 변환이 만들 수 있는 비문
FIXES = [
    ("한 장에 18~30초가 걸립니다", "1건당 18~30초가 소요된다"),
    ("걸립니다", "소요된다"), ("지난다.", "거친다."),
    ("덩어리 단위로는 못 가릅니다", "덩어리 단위로는 구분할 수 없다"),
    ("(코어)을 뽑다", "(코어)을 추출한다"),
    ("물체 자신을 판 것으로 본다", "물체 자체를 제거한 것으로 판단한다"),
    ("당연한다", "당연하다"), ("필요한다", "필요하다"), ("가능한다", "가능하다"),
    ("동일한다", "같다"), ("중요한다", "중요하다"), ("충분한다", "충분하다"),
    ("정확한다", "정확하다"), ("불리한다", "불리하다"), ("유리한다", "유리하다"),
    ("p 는 변하지 않다", "p 는 변하지 않는다"),
    ("값을 내지 않다", "값을 산출하지 않는다"),
    ("정보가 원리적으로 없는 것 이다", "정보가 원리적으로 없는 것이다"),
    ("도련님 ", ""), ("도련님", ""),
    ("실사진 10장", "실거래 사진 10건"), ("표본 10장", "표본 10건"),
    ("사진 10장", "사진 10건"), ("10장 중", "10건 중"), ("8장이 실패", "8건에서 분리에 실패"),
]


def add_sections(s: str, m: dict, n: dict) -> str:
    """0.1.1 태그별 성능 · mIoU 분포 · 12장 · 13장을 삽입한다."""
    tag = f"""<h3>0.1.1 시험항목 1 태그별 성능</h3>

<p>
답안지 원자료(리뷰 100건 × 태그 5종)로 판정값을 재계산하여 기재값과 일치함을 확인하였다.
TP {n['tp']} · FP {n['fp']} · FN {n['fn']} 이며, Precision {n['precision']:.4f},
재현율 {n['recall']:.4f}, 완전일치 {n['exact']}/{n['n']}건이다.
</p>

<div class="tw">
<table>
  <thead><tr><th>태그</th><th class="n">TP</th><th class="n">FP</th><th class="n">FN</th>
      <th class="n">정밀도</th><th class="n">재현율</th></tr></thead>
  <tbody>
    <tr><td>NEGATIVE 부정</td><td class="n">5</td><td class="n">0</td><td class="n">0</td><td class="n">1.000</td><td class="n">1.000</td></tr>
    <tr><td>PRICE 가격</td><td class="n">25</td><td class="n">1</td><td class="n">1</td><td class="n">0.962</td><td class="n">0.962</td></tr>
    <tr><td>FAST 신속</td><td class="n">21</td><td class="n">1</td><td class="n">0</td><td class="n">0.955</td><td class="n">1.000</td></tr>
    <tr><td>KIND 친절</td><td class="n">38</td><td class="n">7</td><td class="n">2</td><td class="n">0.844</td><td class="n">0.950</td></tr>
    <tr><td>해당없음</td><td class="n">23</td><td class="n">0</td><td class="n">6</td><td class="n">1.000</td><td class="n">0.793</td></tr>
    <tr><td>합계</td><td class="n">{n['tp']}</td><td class="n">{n['fp']}</td><td class="n">{n['fn']}</td>
        <td class="n">{n['precision']:.3f}</td><td class="n">{n['recall']:.3f}</td></tr>
  </tbody>
</table>
</div>

<p class="dim">
오분류 {n['fp']}건 가운데 7건이 KIND 오탐이다. 태그가 없는 리뷰에 친절을 부여하는 사례가
대부분이며, 나머지 태그의 정밀도는 0.955 이상이다. 부정 태그는 표본이 5건으로 적으나
오분류 없이 전량 정확히 분류되었다.
</p>

"""
    s = s.replace("<h3>0.2 자체 모델 학습 시도 및 미채택 사유</h3>", tag + "<h3>0.2 자체 모델 학습 시도 및 미채택 사유</h3>", 1)

    stat = f"""<div class="note">
  <p><b>측정값의 분포</b></p>
  <p>10건의 IoU 는 평균 {m['mean']:.2f}%, 표준편차 {m['sd']:.2f}%p 이며 최소 {m['min']:.2f}%,
  최대 {m['max']:.2f}%, 중앙값 {m['median']:.2f}% 로 나타났다. 표본 평균의 95% 신뢰구간은
  {m['ci_lo']:.2f} ~ {m['ci_hi']:.2f}% 이다(t분포, 자유도 9). 구간 하한이 판정기준 70% 를
  상회하므로 표본 수가 10건임을 감안하더라도 기준 충족으로 판단할 수 있으며, 개별 건
  기준으로도 70% 를 하회한 사례는 없다.</p>
  <p>재현율 평균은 {m['recall']:.2f}%, 정밀도 평균은 {m['precision']:.2f}% 로, 정답 영역을 놓치기보다
  배경을 다소 넓게 포함하는 경향을 보인다. 최저치는 T152(커플링 2점)의 {m['min']:.2f}% 이며,
  두 반지가 접해 있어 경계 분리가 어려운 사례이다.</p>
</div>

"""
    s = s.replace("<h3>0.5 거리 산출 결과</h3>", stat + "<h3>0.5 거리 산출 결과</h3>", 1)
    return s.replace("<footer>", APPENDIX + "\n<footer>", 1)


APPENDIX = Path(__file__).with_name("report_sections_12_13.html").read_text() \
    if Path(__file__).with_name("report_sections_12_13.html").exists() else ""


def main() -> None:
    s = SRC.read_text()
    m = json.loads(STATS.read_text())
    n = json.loads(NLP.read_text())

    # 1) 제목·판번호
    s = s.replace("<title>Archimedes — 외곽선 추출·거리 추정 알고리즘 설명서</title>",
                  "<title>TIPS 기능 심사 — 모델·데이터셋·연산과정 명세</title>")
    s = s.replace("<h1>Archimedes — 귀금속 외곽선 추출·거리 추정 알고리즘 설명서</h1>",
                  "<h1>TIPS 기능 심사 — 모델·데이터셋·연산과정 명세</h1>")
    s = re.sub(r'<p class="sub">.*?</p>',
               '<p class="sub">시험항목 4종의 사용 모델, 학습 데이터 출처, 단계별 연산 과정 및 실측 결과</p>',
               s, count=1, flags=re.S)
    s = re.sub(r'<p class="meta">.*?</p>',
               '<p class="meta">주식회사 골드앤컴퍼니 · 2026-08-31 · '
               '근거 답안지 TIPS_시험답안지_골드앤컴퍼니_v9.xlsx · 실측 표본 사진 10건 · 리뷰 100건</p>',
               s, count=1, flags=re.S)
    s = re.sub(r'<p class="lead">.*?</p>',
               '<p class="lead">연구개발 과제 완료판정 시험에 대응해, 각 시험항목에서 사용한 모델과 '
               '그 학습 데이터의 출처, 처리 단계별 연산 과정, 그리고 실제 데이터로 측정한 결과를 '
               '정리하였다. 문서에 실린 자체 측정값은 모두 실측치이다. 사전학습 모델의 학습 규모는 '
               '원저작자가 공개한 수치를 인용하였으며, 해당 모델을 본 과제에서 재학습하지 아니하였다.</p>',
               s, count=1, flags=re.S)

    # 2~4)
    s = rewrite_prose(s)
    for a, b in NOTE_TITLES:
        s = s.replace(f"<b>{a}</b>", f"<b>{b}</b>")
    for a, b in TONE:
        s = s.replace(a, b)
    for a, b in HEADINGS:
        for h in ("h2", "h3", "h4"):
            s = s.replace(f"{a}</{h}>", f"{b}</{h}>")
    s = re.sub(r'(<span class="num">\d+</span>)', r"\1 ", s)

    # 이모지·줄표 정리
    s = s.replace("⚠️ ", "").replace("⚠️", "").replace("⭐ ", "").replace("⭐", "")
    s = re.sub(r"(다|음|함|임)\s+—\s+", r"\1. ", s)

    # 5) 절 추가
    s = add_sections(s, m, n)

    # 6) 비문 교정
    for a, b in FIXES:
        s = s.replace(a, b)
    # 줄바꿈이 끼어 단순 치환으로 안 잡히는 잔여 구어체
    s = re.sub(r"분리에 실패</b>\s*했다", "분리에 실패</b>하였다", s)
    s = re.sub(r"정확도를 숫자로 검증하지\s*못했다", "정확도를 정량적으로 검증하지 못하였다", s)
    s = s.replace("Archimedes · 귀금속 외곽선 추출·거리 추정 알고리즘 설명서",
                  "주식회사 골드앤컴퍼니 · TIPS 기능 심사 자료")

    # 부록: 사진 10건의 픽셀·거리 수치 (요구사항 — 캡처와 수치를 함께 제시)
    d = json.loads(Path("/Users/sihwan/project-Archimedes/web/public/spec/outline.json").read_text())
    KOR = {"ring": "반지", "necklace": "목걸이", "earring": "귀걸이", "goldbar": "골드바"}
    order = ["T379_01", "T374_01", "T390_01", "T332_01", "T338_01",
             "T384_01", "T341_01", "T192_01", "T330_01", "T152_01"]
    rows = []
    for i, k in enumerate(order, 1):
        v = d[k]; dd = v["dist"]
        rows.append(
            f"    <tr><td>{i}</td><td><b>{k.replace('_01','')}</b></td>"
            f"<td class=\"dim\">{v['item'][:24]}</td>"
            f"<td class=\"n\">{v['gt_px']:,}</td><td class=\"n\">{v['model_px']:,}</td>"
            f"<td class=\"n\">{v['inter_px']:,}</td><td class=\"n\">{v['iou']:.2f}%</td>"
            f"<td class=\"n\">{v['recall']:.1f}%</td><td class=\"n\">{v['precision']:.1f}%</td>"
            f"<td class=\"n\">{dd['cm']} cm</td><td class=\"n\">{dd['lo']}~{dd['hi']}</td>"
            f"<td class=\"n\">±{dd['sig']}%</td></tr>")
    appendix = (
        '<h2><span class="num">14</span> 부록. 실거래 사진 10건 측정값</h2>\n\n'
        '<p>시험항목 2 의 픽셀 대조 결과와 시험항목 3(참고)의 거리 산출 결과를 '
        '건별로 정리하였다. 각 건의 원본·추출 결과·정답 대조 이미지는 첨부 문서에 수록하였다.</p>\n\n'
        '<div class="tw">\n<table>\n'
        '  <thead><tr><th>No</th><th>이미지</th><th>품목</th>'
        '<th class="n">정답(px)</th><th class="n">모델(px)</th><th class="n">교집합</th>'
        '<th class="n">IoU</th><th class="n">재현율</th><th class="n">정밀도</th>'
        '<th class="n">거리</th><th class="n">범위(cm)</th><th class="n">오차</th></tr></thead>\n'
        '  <tbody>\n' + "\n".join(rows) + '\n  </tbody>\n</table>\n</div>\n\n')
    def embed(path: Path) -> str:
        """이미지를 문서 안에 담는다. 외부 파일 참조는 메일로 보내면 깨진다."""
        mime = "image/png" if path.suffix == ".png" else "image/jpeg"
        return f"data:{mime};base64," + base64.b64encode(path.read_bytes()).decode()

    # 부록 A 이미지 — 사진 10건의 원본·추출·대조
    figs = []
    for i, k in enumerate(order, 1):
        v = d[k]
        trio = [SPEC / f"{k}_{sfx}.jpg" for sfx in ("orig", "overlay", "diff")]
        if not all(f.exists() for f in trio):
            continue
        cells = "".join(
            f'<figure><img src="{embed(f)}" alt="{lab}"><figcaption>{lab}</figcaption></figure>'
            for f, lab in zip(trio, ("원본", "모델 추출 영역", "정답 대조")))
        dd = v["dist"]
        figs.append(
            f'<div class="apx"><p class="apx-t">{i}. {k.replace("_01","")} {v["item"]}</p>'
            f'<div class="shots3">{cells}</div>'
            f'<p class="apx-n">시험항목 2 (픽셀) — 정답 {v["gt_px"]:,} px · 모델 {v["model_px"]:,} px · '
            f'교집합 {v["inter_px"]:,} px · 누락 {v["miss_px"]:,} px · 과다 {v["over_px"]:,} px · '
            f'IoU {v["iou"]:.2f}% · 재현율 {v["recall"]:.1f}% · 정밀도 {v["precision"]:.1f}%</p>'
            f'<p class="apx-n">시험항목 3 (거리, 참고) — 가정 품목 {KOR.get(dd["k"],"")} · '
            f'추정 {dd["cm"]} cm · 범위 {dd["lo"]}~{dd["hi"]} cm · 오차 ±{dd["sig"]}% · '
            f'원본 {v["w"]}×{v["h"]} · 거래일 {v["date"]}</p></div>')
    if figs:
        appendix += ('<h2><span class="num">15</span> 부록 A. 실거래 사진 10건 대조 결과</h2>\n\n'
                     + "\n".join(figs) + "\n\n")

    # 부록 B: 관리자 화면 캡처
    shots = Path("/Users/sihwan/project-Archimedes/web/public/spec/shots")
    lab = shots / "labels.json"
    if lab.exists():
        L = json.loads(lab.read_text())
        blocks = "\n".join(
            f'  <figure class="shot"><img src="{embed(shots / (n + ".png"))}" alt="{t}">'
            f'<figcaption>{t}</figcaption></figure>'
            for n, t in sorted(L.items()))
        appendix += (
            '<h2><span class="num">16</span> 부록 B. 관리자 화면 검증 캡처</h2>\n\n'
            '<p>운영 중인 관리자 화면에서 시험항목 1(리뷰 태그 분류)의 정확도 집계와 '
            '정답지 대조 결과, 시험항목 4(매칭 점수)의 거래별 산출 결과를 직접 확인한 화면이다.</p>\n\n'
            + blocks + "\n\n")
    s = s.replace("<footer>", appendix + "<footer>", 1)

    s = s.replace("</style>", """
.apx { margin: 1.6rem 0; page-break-inside: avoid; }
.apx-t { font-weight: 650; margin: 0 0 .4rem; }
.apx-n { font-size: .85rem; color: #4a4f5a; margin: .3rem 0 0; }
.shots3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: .6rem; }
.shots3 figure { margin: 0; }
.shots3 img { width: 100%; border: 1px solid #e3e6ec; border-radius: 5px; display: block; }
.shots3 figcaption { font-size: .75rem; opacity: .65; text-align: center; margin-top: .2rem; }
.shot { margin: 1.1rem 0; page-break-inside: avoid; }
.shot img { width: 100%; border: 1px solid #e3e6ec; border-radius: 5px; display: block; }
.shot figcaption { font-size: .85rem; opacity: .7; margin-top: .35rem; }
</style>""", 1)

    DST.write_text(s)

    plain = re.sub(r"<[^>]+>", " ", re.sub(r"<style.*?</style>", "", s, flags=re.S))
    plain = re.sub(r"\s+", " ", plain)
    bad = [w for w in ("습니다", "했다", "당연한다", "필요한다", "동일한다", "도련님") if w in plain]
    print(f"생성: {DST.name}  ({len(s):,} bytes)")
    print(f"비문·구어체 잔존: {bad if bad else '없음'}")


if __name__ == "__main__":
    main()
